# TLS и HTTP->HTTPS redirect выполняет внешний Traefik. До Nginx запрос
# приходит по HTTP с X-Forwarded-Proto=https.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name finntrail.ru www.finntrail.ru;

    include /etc/nginx/inc/bitrix-server.conf;
}
